/** @type {import('tailwindcss').Config} */
module.exports = {
  // v4 doesn't require the `content` array anymore.
  theme: {
    extend: {
      colors: {
        brand: "#56A3A6", // teal
        sand:  "#E1CA96", // warm gold
      },
    },
  },
  plugins: [],
};
